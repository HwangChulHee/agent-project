# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# === uv / 가상환경 ===
.venv/
venv/
env/
.python-version-cache

# === IDE / 에디터 ===
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# === 환경변수 / 비밀 ===
# .env 파일은 본인 결정으로 커밋함 (vLLM 로컬 주소만 있음)
# 진짜 비밀 (HF 토큰 등)은 절대 커밋하지 말 것
.env.local
.env.*.local
*.pem
*.key
hf_token
hf_token.txt

# === HuggingFace 캐시 (모델 파일은 별도 위치에 있음) ===
# 실제로는 /workspace/hf-cache/에 있어서 이 리포에 안 들어옴, 안전망용
hf-cache/
*.safetensors
*.gguf

# === 로그 / 임시 파일 ===
*.log
*.tmp
*.bak
*~

# === uv 캐시 ===
.uv-cache/
